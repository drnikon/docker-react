sudo: required
env:
  - CI=true
services:
  - docker

before_install:
  - docker build -t drnikon/docker-react -f Dockerfile.dev .

script:
  - docker run drnikon/docker-react npm run test -- --coverage
